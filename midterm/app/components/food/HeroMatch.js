"use client";
import { useState } from "react";
import styles from "../../page.module.css";

export default function HeroMatch(hreo) {
  return (
    <div className={styles.HeroMatchWrapper}>
      <p>hero</p>
    </div>
  );
}
